info:
  name: selectAllAutores
  type: http
  seq: 8

http:
  method: GET
  url: "{{local}}/autores/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
