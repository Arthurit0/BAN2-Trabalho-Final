info:
  name: selectBanda
  type: http
  seq: 2

http:
  method: GET
  url: "{{local}}/bandas/:nrReg"
  params:
    - name: nrReg
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
