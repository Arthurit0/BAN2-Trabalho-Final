info:
  name: deleteDisco
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{local}}/discos/:cdDisco"
  params:
    - name: cdDisco
      value: "5"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
