info:
  name: selectDisco
  type: http
  seq: 2

http:
  method: GET
  url: "{{local}}/discos/:cdDisco"
  params:
    - name: cdDisco
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
