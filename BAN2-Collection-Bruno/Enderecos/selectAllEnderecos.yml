info:
  name: selectAllEnderecos
  type: http
  seq: 1

http:
  method: GET
  url: "{{local}}/enderecos/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
