info:
  name: deleteEstudio
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{local}}/estudios/:cdEstudio"
  params:
    - name: cdEstudio
      value: "5"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
