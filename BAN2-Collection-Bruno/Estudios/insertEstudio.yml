info:
  name: insertEstudio
  type: http
  seq: 3

http:
  method: POST
  url: "{{local}}/estudios/"
  body:
    type: json
    data: |-
      {
          "cdEndereco": 3,
          "nmEstudio": "Famosão"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
