info:
  name: selectAllEstudios
  type: http
  seq: 1

http:
  method: GET
  url: "{{local}}/estudios/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
