info:
  name: selectEstudio
  type: http
  seq: 2

http:
  method: GET
  url: "{{local}}/estudios/:cdEstudio"
  params:
    - name: cdEstudio
      value: "3"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
