info:
  name: updateEstudio
  type: http
  seq: 4

http:
  method: PUT
  url: "{{local}}/estudios/:cdEstudio"
  params:
    - name: cdEstudio
      value: "5"
      type: path
  body:
    type: json
    data: |-
      {
          "cdEndereco": 3,
          "nmEstudio": "Famosão Mesmo!!!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
