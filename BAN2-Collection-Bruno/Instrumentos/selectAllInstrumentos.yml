info:
  name: selectAllInstrumentos
  type: http
  seq: 1

http:
  method: GET
  url: "{{local}}/instrumentos/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
