info:
  name: selectInstrumento
  type: http
  seq: 2

http:
  method: GET
  url: "{{local}}/instrumentos/:cdInstrumento"
  params:
    - name: cdInstrumento
      value: "2"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
