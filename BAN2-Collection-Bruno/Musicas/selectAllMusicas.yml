info:
  name: selectAllMusicas
  type: http
  seq: 1

http:
  method: GET
  url: "{{local}}/musicas/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
