info:
  name: selectMusica
  type: http
  seq: 2

http:
  method: GET
  url: "{{local}}/musicas/:cdMusica"
  params:
    - name: cdMusica
      value: "2"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
