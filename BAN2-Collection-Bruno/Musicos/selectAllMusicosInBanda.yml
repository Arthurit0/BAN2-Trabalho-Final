info:
  name: selectAllMusicosInBanda
  type: http
  seq: 9

http:
  method: GET
  url: "{{local}}/musicos-in-bandas"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
