info:
  name: selectMusico
  type: http
  seq: 2

http:
  method: GET
  url: "{{local}}/musicos/:nrReg"
  params:
    - name: nrReg
      value: "2"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
