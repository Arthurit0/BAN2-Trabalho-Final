info:
  name: updateMusico
  type: http
  seq: 4

http:
  method: PUT
  url: "{{local}}/musicos/:nrReg"
  params:
    - name: nrReg
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
